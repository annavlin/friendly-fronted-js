const selectors = {}; // экспортировали из одного файла в другой
export default function initTabs() {
  console.log('initTabs');
}

// либо же можно записать так, экспортируем все, что хотим и записываем в фигурные скобки

// export { tabsSelectors };
